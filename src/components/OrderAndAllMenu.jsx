import React from 'react';
import '../assets/styles/components/OrderAndAllMenu.scss';
const OrderAndAllMenu = ({children}) => (
    <section className="orderAndAllMenu">
        {children}
    </section>
)

export default OrderAndAllMenu;