import React from 'react';
import '../assets/styles/components/OptionsAllMenu.scss'
const OptionsAllMenu = ({children}) => (
    <nav class="optionsAllMenu">
        {children}
    </nav>
)

export default OptionsAllMenu;